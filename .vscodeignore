# Exclude source files and dev-only files
src/
.vscode/
*.ts
*.map
*.tsbuildinfo
tsconfig.json

# Exclude test files and related
test/
*.test.ts

# Exclude node_modules (will be rebuilt on install)
node_modules/

# Misc
.vscodeignore
package-lock.json
yarn.lock
*.log

# Don't bundle your own VSIX output
*.vsix
